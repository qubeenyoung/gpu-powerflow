# Build trees (out-of-source build lives in /tmp/profile-build)
/build/
build/
*.o
*.so
*.a

# Python
__pycache__/
*.pyc

# Nsight captures can be large; keep them out of git
*.nsys-rep
*.ncu-rep
*.qdrep
*.sqlite

# Transient Discord retry queue (sent reports are kept under discord_sent/)
report/autorun/log/discord_outbox/
