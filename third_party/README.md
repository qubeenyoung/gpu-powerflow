# Third Party Dependencies

Large external source trees, local builds, and installed solver dependencies live
under this directory during experiments.

The directory contents are intentionally ignored by git. Recreate dependencies
from their upstream sources or local install scripts when needed.
