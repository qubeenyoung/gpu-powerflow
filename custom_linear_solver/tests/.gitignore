__pycache__/
*.pyc

# Root .gitignore ignores any "datasets/"; re-include this one so the power-grid
# Jacobians (small, canonical benchmark set) are tracked...
!datasets/
# ...but keep the large SuiteSparse matrices out — fetched by
# datasets/fetch_suitesparse.sh on demand.
datasets/suitesparse/

# Build outputs (regenerated by scripts/build_*.sh)
scripts/cudss_bench
scripts/cudss_bench2
scripts/strumpack_bench_magma
scripts/strumpack_bench_nomagma
