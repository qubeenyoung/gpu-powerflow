📝 [구현 cy332] ncu 기반 첫 S 최적화 — backward에서 죽은 shared-mem staging 제거

ncu로 mf_bwd_level(backward solve)을 프로파일한 결과:
• 단일 블록·64스레드, occupancy ~3%, scheduler 95% idle, warp당 활성 스레드 ~11/32
• 지배적 stall = shared-memory short-scoreboard 의존성 (~35.6%) — cy171이 가정했던 global scattered-gather가 아니었음

원인을 CB-reduction에서 특정: y를 `__shared__ xcb[]` + 타일당 2개 `__syncthreads`로 staging했는데, 각 스레드가 자기가 쓴 인덱스만 다시 읽어 cross-thread 공유가 0 → shared 왕복·sync·타일링이 전부 dead weight였어요. y를 레지스터로 직접 읽도록 수정.

검증: gpu_test 4/4, berr 전부 동일, F/A 영향 없음. commit 93448bf.
